name: Backend CI/CD 🚀

on:
  push:
    branches: ["deploy"]
    paths:
      - "backend/**"
      - "!frontend/**"
  pull_request:
    branches: ["deploy"]
    paths:
      - "backend/**"
  workflow_dispatch:

concurrency:
  group: backend-cicd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: self-hosted

    env:
      PRPPENU_GATEWAY_ENV: ${{ secrets.PRPPENU_GATEWAY_ENV }}
      PRPPENU_PAYMENT_ENV: ${{ secrets.PRPPENU_PAYMENT_ENV }}
      PRPPENU_PROPERTY_ENV: ${{ secrets.PRPPENU_PROPERTY_ENV }}
      PRPPENU_USER_ENV: ${{ secrets.PRPPENU_USER_ENV }}

    defaults:
      run:
        shell: bash

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Find actual lockfiles (if any). We'll only enable cache when we have ≥1.
      - name: Find lockfiles
        id: lock
        run: |
          set -e
          files="$(find backend -type f -name package-lock.json | sed 's|^./||' || true)"
          if [ -n "$files" ]; then
            # Save as a newline-separated output
            {
              echo "paths<<EOF"
              echo "$files"
              echo "EOF"
            } >> "$GITHUB_OUTPUT"
            echo "Found lockfiles:"
            echo "$files"
          else
            echo "No lockfiles found."
            echo "paths=" >> "$GITHUB_OUTPUT"
          fi

      # Setup Node with cache ONLY when we have valid paths
      - name: Setup Node.js ${{ matrix.node-version }} (cached)
        if: ${{ steps.lock.outputs.paths != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
          cache-dependency-path: ${{ steps.lock.outputs.paths }}

      # Fallback: Setup Node without cache
      - name: Setup Node.js ${{ matrix.node-version }} (no cache)
        if: ${{ steps.lock.outputs.paths == '' }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Show runner workspace
        run: |
          echo "GITHUB_WORKSPACE = $GITHUB_WORKSPACE"
          pwd
          ls -la
          echo "Backend tree:"
          ls -la backend || true

      - name: Check Secrets
        run: |
          check() { [ -n "$2" ] && echo "$1: ✅ OK" || (echo "$1: ❌ MISSING"; exit 1); }
          check PRPPENU_GATEWAY_ENV "${PRPPENU_GATEWAY_ENV}"
          check PRPPENU_PAYMENT_ENV "${PRPPENU_PAYMENT_ENV}"
          check PRPPENU_PROPERTY_ENV "${PRPPENU_PROPERTY_ENV}"
          check PRPPENU_USER_ENV "${PRPPENU_USER_ENV}"

      # Write .env files (use printf so the secret expands)
      - name: Write gateway .env
        working-directory: backend/gateway
        run: |
          printf "%s" "${PRPPENU_GATEWAY_ENV}" > .env
          echo "✅ gateway .env created"

      - name: Write service .env files
        run: |
          write_env () { printf "%s" "$1" > "$2" && echo "✅ wrote $2"; }
          write_env "${PRPPENU_PAYMENT_ENV}" backend/services/payment-service/.env
          write_env "${PRPPENU_PROPERTY_ENV}" backend/services/property-service/.env
          write_env "${PRPPENU_USER_ENV}" backend/services/user-service/.env

      - name: Verify gateway .env exists
        working-directory: backend/gateway
        run: |
          test -s .env && echo "✅ .env present" || (echo "❌ .env missing"; exit 1)

      # Install & build gateway
      - name: Install gateway deps
        working-directory: backend/gateway
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit --no-fund
          else
            echo "⚠️ package-lock.json missing; using npm install"
            npm install --no-audit --no-fund
          fi

      - name: Build gateway
        working-directory: backend/gateway
        run: |
          if npm run | grep -qE '(^|\s)build(\s|:)'; then
            npm run build
          else
            echo "⚠️ No build script for gateway."
          fi

      - name: Prune gateway dev deps
        working-directory: backend/gateway
        run: npm prune --omit=dev || true

      # Install & build all services
      - name: Install service deps
        run: |
          for dir in backend/services/*-service; do
            [ -d "$dir" ] || continue
            if [ -f "$dir/package.json" ]; then
              echo "Installing deps in $dir"
              if [ -f "$dir/package-lock.json" ]; then
                (cd "$dir" && npm ci --no-audit --no-fund)
              else
                echo "⚠️ $dir has no package-lock.json; using npm install"
                (cd "$dir" && npm install --no-audit --no-fund)
              fi
            else
              echo "⚠️ No package.json in $dir, skipping install"
            fi
          done

      - name: Build services
        run: |
          for dir in backend/services/*-service; do
            [ -d "$dir" ] || continue
            if (cd "$dir" && npm run | grep -qE '(^|\s)build(\s|:)'); then
              echo "Building $dir"
              (cd "$dir" && npm run build)
            else
              echo "⚠️ No build script in $dir"
            fi
          done

      - name: Prune service dev deps
        run: |
          for dir in backend/services/*-service; do
            [ -d "$dir" ] || continue
            (cd "$dir" && npm prune --omit=dev) || true
          done

      # PM2 reload
      - name: Reload PM2 apps
        working-directory: backend
        run: |
          if ! command -v pm2 >/dev/null 2>&1; then
            echo "❌ PM2 not found. Install: npm i -g pm2"
            exit 1
          fi

          if [ ! -f ecosystem.config.cjs ]; then
            echo "❌ Missing ecosystem.config.cjs"
            exit 1
          fi

          echo "🚀 Reloading PM2..."
          pm2 start ecosystem.config.cjs || true
          pm2 reload ecosystem.config.cjs
          pm2 save
          pm2 status
